[[examples]]
task = "Deploy a connector with Snowflake Openflow"
operations = [
    {name = "Create service user",                        type = "sql"},
    {name = "Create service role",                        type = "sql"},
    {name = "Grant role to user",                         type = "sql"},
    {name = "Create network rule",                        type = "sql"},
    {name = "Create network policy",                      type = "sql"},
    {name = "Attach network policy to user",              type = "sql"},
    {name = "Create authentication policy",               type = "sql"},
    {name = "Attach authentication policy to user",       type = "sql"},
    {name = "Configure PAT policy constraints",           type = "config"},
    {name = "Generate PAT",                               type = "sql"},
    {name = "Store PAT securely for client usage",        type = "config"},
    {name = "Validate token exchange via Apache NiPyAPI", type = "python"},
]

[examples.prompts]
verbose = """I need to deploy a connector using Snowflake Openflow. \
First, I need a service user created in Snowflake. The user should be a service account type. \
Then I need a role created for this service user. The role should be granted to the user. \
After that, I need network rules configured to allow access to the external endpoint. \
The network rule should be wrapped in a network policy. The network policy needs to be \
attached to the service user. Then I need an authentication policy that references the \
correct role. The auth policy also needs to be attached to the user. After all of that, \
I need PAT policy constraints configured, and then a programmatic access token generated. \
The PAT should be stored securely. Finally, validate the token works with NiPyAPI. \
Please make sure each step is done in the correct order because the dependencies matter. \
The network policy must exist before it's attached. The auth policy must reference the \
correct role. The PAT must be generated after all constraints are satisfied."""
clarification_heavy = [
    "Can you help me set up Openflow?",
    "It's a Snowflake connector. I need to deploy one.",
    "It connects to Google Cloud Storage.",
    "I think I need a PAT for authentication.",
    "Yes, there should be network policies too.",
    "The service user needs the right role and policies attached.",
    "OK, and the PAT needs to be generated last, after everything else is configured.",
    "Right, go ahead and set it all up in the correct order.",
]
context_aware = "Deploy a Snowflake Openflow connector to GCS. Set up the service user with appropriate network and auth policies, then generate a PAT for NiPyAPI client authentication."
intent_optimized = "Deploy Openflow connector: GCS target, PAT auth, NiPyAPI client."
over_compressed = "Deploy connector."
assumption_led = [
    "Assuming GCS target, PAT authentication, and NiPyAPI client. Proceeding with full Openflow deployment.",
    "Correction: target is Azure Blob Storage, not GCS.",
    "Re-executing with Azure Blob Storage target and updated endpoint configuration.",
]

[[examples]]
task = "Deploy payment service with autoscaling and observability"
operations = [
    {name = "Build container image",              type = "bash"},
    {name = "Push to registry",                   type = "bash"},
    {name = "Create Kubernetes deployment",       type = "bash"},
    {name = "Configure HPA autoscaling",          type = "config"},
    {name = "Set up service and ingress",          type = "bash"},
    {name = "Deploy Prometheus metrics exporter", type = "bash"},
    {name = "Configure Grafana dashboards",       type = "config"},
    {name = "Set up alerting rules",              type = "config"},
    {name = "Configure distributed tracing",      type = "config"},
]

[examples.prompts]
verbose = """I need to deploy our payment service. It's a containerized application that needs to \
be deployed to Kubernetes. First, build the Docker image from the Dockerfile in the \
repo. Then push it to our container registry. Create a Kubernetes deployment with the \
image. I also need autoscaling — use a Horizontal Pod Autoscaler that scales based on \
CPU and memory. Set up a Service and Ingress so it's accessible. For observability, \
I need Prometheus metrics exported from the service, Grafana dashboards configured to \
show payment transaction metrics, alerting rules for error rates and latency, and \
distributed tracing with Jaeger. Make sure the deployment has proper resource limits \
and health checks configured. The autoscaler should scale between 2 and 10 replicas."""
clarification_heavy = [
    "I need to deploy the payment service.",
    "Yes, to Kubernetes. It needs autoscaling.",
    "HPA based on CPU and memory. Min 2, max 10 replicas.",
    "Also need observability. Prometheus and Grafana.",
    "Yes, and alerting. Plus tracing with Jaeger.",
    "The image needs to be built and pushed first.",
]
context_aware = "Deploy payment-service to k8s with HPA (2-10 pods, CPU/memory). Include full observability stack: Prometheus metrics, Grafana dashboards, alerting rules, and Jaeger tracing."
intent_optimized = "Deploy payment-service: k8s, HPA 2-10, full observability (metrics/dashboards/alerts/tracing)."
over_compressed = "Deploy payment service to k8s."
assumption_led = [
    "Assuming Docker Hub registry, Kubernetes cluster available, and Prometheus/Grafana stack installed. Proceeding with full deployment.",
    "Correction: registry is ECR, not Docker Hub; Grafana is replaced by Datadog.",
    "Re-executing with ECR registry and Datadog for dashboards and alerting.",
]

[[examples]]
task = "Set up an Iceberg table with external volume"
operations = [
    {name = "Create storage integration", type = "sql"},
    {name = "Configure IAM roles",        type = "config"},
    {name = "Create external volume",     type = "sql"},
    {name = "Validate access",            type = "sql"},
    {name = "Create catalog integration", type = "sql"},
    {name = "Create Iceberg table",       type = "sql"},
]

[examples.prompts]
verbose = """I want to set up an Apache Iceberg table in Snowflake that uses an external volume. \
First, I need a storage integration created that points to our S3 bucket. \
The S3 bucket is in us-east-1. I'll need the IAM role configured with the right \
trust policy so Snowflake can assume it. Then create an external volume using the \
storage integration. Make sure to validate that Snowflake can actually access the \
bucket by checking the storage integration properties and doing a test. Then set up \
a catalog integration if needed. Finally, create the Iceberg table using the external \
volume as storage. The table should be in my analytics database."""
clarification_heavy = [
    "I need an Iceberg table.",
    "On S3. In us-east-1.",
    "I need an external volume for it.",
    "The IAM role? Let me check... yes, I'll need a trust policy.",
    "The table goes in the analytics database.",
    "And yes, validate access works before creating the table.",
]
context_aware = "Create Iceberg table in ANALYTICS db backed by external volume on S3 (us-east-1). Handle storage integration, IAM, and access validation."
intent_optimized = "Iceberg table: ANALYTICS db, S3 external volume (us-east-1), validate access."
over_compressed = "Create Iceberg table on S3."
assumption_led = [
    "Assuming S3 bucket in us-east-1, IAM role pre-configured, and ANALYTICS database exists. Proceeding with Iceberg table setup.",
    "Correction: storage backend is GCS, not S3.",
    "Re-executing with GCS external volume and updated storage integration.",
]

[[examples]]
task = "Refactor authentication module to use OAuth2"
operations = [
    {name = "Audit current auth implementation", type = "python"},
    {name = "Design OAuth2 flow",                type = "config"},
    {name = "Implement token endpoint",          type = "python"},
    {name = "Add refresh token logic",           type = "python"},
    {name = "Update middleware",                 type = "python"},
    {name = "Migrate session handling",          type = "python"},
    {name = "Update API clients",                type = "python"},
    {name = "Add PKCE support",                  type = "python"},
]

[examples.prompts]
verbose = """I need to refactor our authentication module. Currently it uses session-based auth \
with cookies, but we need to move to OAuth2. I need you to first audit the current \
implementation to understand all the places auth is used. Then design the OAuth2 flow — \
we want authorization code flow with PKCE. Implement a token endpoint that issues \
access and refresh tokens. Add logic for refresh token rotation. Update the auth \
middleware to validate JWT tokens instead of sessions. Migrate the session handling \
to use the new token-based approach. Update all API clients that currently send \
session cookies to use Bearer tokens instead. Make sure PKCE is implemented properly \
for public clients."""
clarification_heavy = [
    "We need to update our auth.",
    "Moving from sessions to OAuth2.",
    "Authorization code flow.",
    "Yes, with PKCE for public clients.",
    "Refresh tokens too, with rotation.",
    "All API clients need updating.",
    "The middleware needs to validate JWTs now.",
]
context_aware = "Refactor auth from session-based to OAuth2 (authorization code + PKCE). Implement token endpoint with refresh rotation, update middleware to JWT validation, and migrate all API clients to Bearer tokens."
intent_optimized = "Refactor auth -> OAuth2: authz code + PKCE, JWT middleware, refresh rotation, migrate clients."
over_compressed = "Switch auth to OAuth2."
assumption_led = [
    "Assuming authorization code flow with PKCE, all clients support modern OAuth2, and JWT for all tokens. Proceeding with OAuth2 refactor.",
    "Correction: legacy clients cannot use PKCE; they need client credentials flow.",
    "Re-executing with PKCE for public clients and client credentials flow for legacy services.",
]

[[examples]]
task = "Build real-time data pipeline with CDC"
operations = [
    {name = "Configure source database CDC",          type = "config"},
    {name = "Set up Kafka Connect source connector",  type = "config"},
    {name = "Create Kafka topics with schemas",       type = "bash"},
    {name = "Implement stream processing transforms", type = "python"},
    {name = "Configure sink connector",               type = "config"},
    {name = "Set up schema registry",                 type = "bash"},
    {name = "Implement dead letter queue",            type = "python"},
    {name = "Configure monitoring",                   type = "config"},
    {name = "Set up data quality checks",             type = "python"},
    {name = "Implement backfill strategy",            type = "python"},
]

[examples.prompts]
verbose = """I need to build a real-time data pipeline using Change Data Capture. The source is \
a PostgreSQL database. I need CDC enabled on the source tables — use logical \
replication. Then set up a Kafka Connect source connector to stream the changes \
into Kafka. Create the necessary Kafka topics with proper partitioning and retention. \
Register Avro schemas in the schema registry. Implement stream processing to transform \
the CDC events — we need to denormalize some tables and compute aggregates. \
Set up a sink connector to write to the target data warehouse. Implement a dead letter \
queue for failed records. Add monitoring for lag, throughput, and errors. Set up data \
quality checks at the sink. Also implement a backfill strategy for the initial load \
and for cases where we need to replay events."""
clarification_heavy = [
    "I need a CDC pipeline.",
    "Source is PostgreSQL.",
    "Target is our data warehouse.",
    "Using Kafka for streaming.",
    "Yes, with schema registry. Avro format.",
    "Need transforms — denormalization and aggregates.",
    "Also need DLQ and monitoring.",
    "And a backfill strategy for initial load.",
]
context_aware = "Build CDC pipeline: PostgreSQL -> Kafka (Avro/Schema Registry) -> warehouse. Include stream transforms (denorm + aggregates), DLQ, monitoring, and backfill strategy."
intent_optimized = "CDC pipeline: Postgres -> Kafka (Avro) -> warehouse. Transforms, DLQ, monitoring, backfill."
over_compressed = "Set up CDC pipeline."
assumption_led = [
    "Assuming PostgreSQL source with logical replication enabled, Kafka cluster available, and data warehouse as sink. Proceeding with CDC pipeline.",
    "Correction: source database is MySQL, not PostgreSQL.",
    "Re-executing with MySQL CDC using Debezium connector instead of PostgreSQL logical replication.",
]

[[examples]]
task = "Configure network access with security policies"
operations = [
    {name = "Define network rule",           type = "sql"},
    {name = "Create network policy",         type = "sql"},
    {name = "Attach policy to account/user", type = "sql"},
    {name = "Validate connectivity",         type = "bash"},
]

[examples.prompts]
verbose = """I need to configure network access in Snowflake. I want to create a network rule \
that allows access to googleapis.com. The rule should be of type HOST_PORT. Then I \
need a network policy that uses this rule in its allowed list. The policy should be \
attached to the service user we created. After attaching, I need to validate that \
the connectivity actually works by testing from the service account context. Make sure \
the rule is created before the policy references it, and the policy exists before \
it's attached to the user."""
clarification_heavy = [
    "I need network access configured.",
    "To googleapis.com.",
    "It's for a service user.",
    "A network policy with a rule, yes. HOST_PORT type.",
]
context_aware = "Configure network access to googleapis.com for the service user. Create HOST_PORT rule, wrap in policy, attach to user, validate."
intent_optimized = "Network access: allow googleapis.com for service user, validate."
over_compressed = "Allow network access."
assumption_led = [
    "Assuming googleapis.com HOST_PORT rule, service user exists, and account-level policy attachment. Proceeding with network configuration.",
    "Correction: policy should be attached at user level only, not account level.",
    "Re-executing with user-level network policy attachment for the service account.",
]

[[examples]]
task = "Deploy microservices app with service mesh and canary rollouts"
operations = [
    {name = "Create namespace with labels",            type = "bash"},
    {name = "Deploy microservice Deployments",         type = "bash"},
    {name = "Create ClusterIP Services",               type = "bash"},
    {name = "Install Istio sidecar injection",         type = "bash"},
    {name = "Configure VirtualService routing",        type = "config"},
    {name = "Define DestinationRules with subsets",    type = "config"},
    {name = "Set canary weight-based traffic split",   type = "config"},
    {name = "Enable mTLS with PeerAuthentication",     type = "config"},
    {name = "Add circuit breaker policies",            type = "config"},
    {name = "Configure rate limiting with EnvoyFilter", type = "config"},
    {name = "Set up HPA for each service",             type = "config"},
]

[examples.prompts]
verbose = """I need to deploy our microservices application to Kubernetes with a service mesh. \
First, create a namespace with the proper Istio injection labels. Then deploy each \
microservice as a Deployment with the right resource limits and readiness probes. \
Create ClusterIP Services for each. Install Istio sidecar injection so all pods get \
Envoy proxies. Now configure VirtualService routing rules so traffic flows correctly \
between services. Define DestinationRules with subsets for stable and canary versions. \
Set up a canary rollout with weight-based traffic splitting — start at 10% canary. \
Enable mutual TLS between all services using PeerAuthentication. Add circuit breaker \
policies on the DestinationRules to handle failures gracefully. Configure rate limiting \
using an EnvoyFilter. Finally, set up Horizontal Pod Autoscalers for each service \
based on CPU utilization. Make sure Istio is injecting sidecars before you configure \
any mesh policies."""
clarification_heavy = [
    "I need to deploy microservices to Kubernetes.",
    "Yes, with a service mesh. Istio.",
    "We want canary deployments. Weight-based.",
    "Start at 10% traffic to canary.",
    "mTLS between services, yes.",
    "Also circuit breakers and rate limiting.",
    "Each service needs autoscaling too.",
    "HPA on CPU. Default thresholds are fine.",
]
context_aware = "Deploy microservices to k8s with Istio mesh. Configure canary rollouts (10% initial weight), mTLS, circuit breakers, rate limiting, and HPA per service."
intent_optimized = "Microservices on k8s: Istio mesh, canary 10%, mTLS, circuit breakers, rate limits, HPA."
over_compressed = "Deploy microservices with Istio."
assumption_led = [
    "Assuming Istio is installed cluster-wide, canary traffic starts at 10%, and strict mTLS required. Proceeding with mesh deployment.",
    "Correction: use PERMISSIVE mTLS mode to allow gradual migration of non-mesh services.",
    "Re-executing with PERMISSIVE mTLS and updated PeerAuthentication policies.",
]

[[examples]]
task = "Implement a rate limiter with sliding window and distributed state"
operations = [
    {name = "Design sliding window algorithm",                      type = "python"},
    {name = "Implement token bucket core",                         type = "python"},
    {name = "Add sliding window counter with timestamps",          type = "python"},
    {name = "Integrate Redis as distributed backend",              type = "python"},
    {name = "Handle race conditions with Lua atomics",             type = "python"},
    {name = "Add middleware hook for HTTP frameworks",             type = "python"},
    {name = "Emit rate limit metrics (allowed/rejected/remaining)", type = "python"},
    {name = "Write unit and integration tests",                    type = "python"},
]

[examples.prompts]
verbose = """I need to implement a rate limiter for our API. It should use a sliding window \
algorithm, not a fixed window, so we don't get burst issues at window boundaries. \
Start by designing the sliding window approach — I'm thinking a hybrid of fixed \
window counters with interpolation. Implement the core token bucket logic first. \
Then add the sliding window counter that tracks request timestamps. The state needs \
to be distributed, so integrate Redis as the backend store. We'll have multiple \
instances of the service running, so there will be race conditions — use Redis Lua \
scripts to make the check-and-increment atomic. Expose this as a middleware that \
any HTTP handler can use. The middleware should set X-RateLimit headers. Add metrics \
emission so we can track allowed, rejected, and remaining quota in our monitoring. \
Write both unit tests for the algorithm and integration tests against a real Redis."""
clarification_heavy = [
    "I need a rate limiter.",
    "Sliding window, not fixed.",
    "Distributed — multiple service instances.",
    "Using Redis for shared state.",
    "Need to handle race conditions. Lua scripts.",
    "It should work as HTTP middleware.",
    "Yes, with X-RateLimit headers.",
    "And we need tests — unit and integration.",
]
context_aware = "Implement distributed rate limiter: sliding window algorithm, Redis backend with Lua atomics for concurrency, HTTP middleware with X-RateLimit headers, metrics emission, unit + integration tests."
intent_optimized = "Rate limiter: sliding window, Redis + Lua atomics, HTTP middleware, metrics, tests."
over_compressed = "Build a rate limiter."
assumption_led = [
    "Assuming Redis cluster is available, sliding window algorithm preferred, and HTTP middleware integration needed. Proceeding with rate limiter implementation.",
    "Correction: no Redis available; use in-memory state with sticky load balancing.",
    "Re-executing with in-memory sliding window rate limiter suitable for single-instance deployment.",
]

[[examples]]
task = "Provision multi-region AWS infrastructure with DR failover"
operations = [
    {name = "Define provider configs for both regions",           type = "config"},
    {name = "Create VPCs with subnets per region",               type = "config"},
    {name = "Set up RDS primary with cross-region read replica",  type = "config"},
    {name = "Configure Route53 health checks",                   type = "config"},
    {name = "Set up Route53 failover routing policy",            type = "config"},
    {name = "Create S3 buckets with cross-region replication",   type = "config"},
    {name = "Configure CloudFront with multi-origin failover",   type = "config"},
    {name = "Create IAM roles and policies",                     type = "config"},
    {name = "Extract reusable Terraform modules",                type = "config"},
    {name = "Configure remote state with DynamoDB locking",      type = "config"},
]

[examples.prompts]
verbose = """I need to provision multi-region infrastructure on AWS using Terraform. We need \
disaster recovery with automatic failover. First, define provider configurations \
for us-east-1 (primary) and us-west-2 (DR). Create a VPC in each region with \
public and private subnets across multiple AZs. Set up an RDS PostgreSQL instance \
in the primary region with a cross-region read replica in DR. Configure Route53 \
health checks that monitor the primary region's endpoints. Set up a failover \
routing policy so traffic automatically goes to DR if primary is unhealthy. Create \
S3 buckets in both regions with cross-region replication enabled. Set up CloudFront \
with an origin group that fails over to the DR region. Create all necessary IAM \
roles and policies with least-privilege access. Extract common patterns into \
reusable Terraform modules. Use S3 backend with DynamoDB locking for state. \
Make sure the modules are parameterized so we can add more regions later."""
clarification_heavy = [
    "I need multi-region infra on AWS.",
    "For disaster recovery. Automatic failover.",
    "Primary is us-east-1, DR is us-west-2.",
    "Using Terraform.",
    "RDS Postgres with a cross-region replica.",
    "Route53 for DNS failover, yes.",
    "S3 needs cross-region replication too.",
    "And CloudFront in front with origin failover.",
    "Remote state in S3 with DynamoDB locking.",
]
context_aware = "Terraform multi-region AWS (us-east-1 primary, us-west-2 DR): VPCs, RDS with cross-region replica, Route53 failover, S3 CRR, CloudFront origin groups, IAM least-privilege. Modularized with S3+DynamoDB state backend."
intent_optimized = "Terraform: multi-region AWS DR (east-1/west-2), RDS replica, Route53 failover, S3 CRR, CloudFront, modular."
over_compressed = "Terraform multi-region AWS."
assumption_led = [
    "Assuming us-east-1 primary, us-west-2 DR region, RDS PostgreSQL, and S3+DynamoDB Terraform state backend. Proceeding with multi-region infrastructure.",
    "Correction: DR region should be eu-west-1, not us-west-2.",
    "Re-executing with eu-west-1 as the DR region and updated provider configurations.",
]

[[examples]]
task = "Configure hardened Linux fleet with CIS benchmarks and monitoring"
operations = [
    {name = "Create Ansible inventory with host groups",                      type = "config"},
    {name = "Write SSH hardening role (disable root, key-only auth)",        type = "config"},
    {name = "Apply CIS Level 1 benchmark tasks (kernel params, filesystem)", type = "config"},
    {name = "Configure firewalld/iptables rules",                            type = "bash"},
    {name = "Deploy Prometheus node_exporter",                               type = "bash"},
    {name = "Configure Filebeat for centralized logging",                    type = "config"},
    {name = "Set up unattended security upgrades",                           type = "config"},
    {name = "Create users with sudo rules (no NOPASSWD)",                    type = "bash"},
    {name = "Run OpenSCAP compliance scan and generate report",              type = "bash"},
]

[examples.prompts]
verbose = """I need to configure our Linux server fleet using Ansible to meet security \
requirements. First, create an inventory file that groups hosts by role — web \
servers, app servers, and databases. Write a hardening role that disables root \
SSH login, enforces key-only authentication, sets idle timeout, and disables \
password auth. Apply CIS Level 1 benchmark tasks — things like setting kernel \
parameters (disable IP forwarding, enable ASLR), restricting filesystem mounts, \
setting proper permissions on sensitive files. Configure firewall rules using \
firewalld — only allow necessary ports per host group. Deploy Prometheus \
node_exporter on all hosts for metrics collection. Set up Filebeat to ship logs \
to our ELK stack. Configure unattended security upgrades so critical patches \
apply automatically. Create operator users with proper sudo rules — no NOPASSWD, \
require password for privilege escalation. Finally, run an OpenSCAP compliance \
scan to validate everything and generate an HTML report. The playbook should be \
idempotent so we can run it repeatedly."""
clarification_heavy = [
    "I need to harden our Linux servers.",
    "Using Ansible. About 50 hosts.",
    "CIS benchmarks — Level 1.",
    "SSH hardening, kernel params, filesystem restrictions.",
    "Also need monitoring. Prometheus node_exporter.",
    "Logging too — Filebeat to ELK.",
    "Unattended security upgrades, yes.",
    "And run a compliance scan at the end. OpenSCAP.",
]
context_aware = "Ansible playbook for Linux fleet hardening: CIS Level 1 benchmarks, SSH lockdown, firewalld rules by host group, node_exporter + Filebeat agents, unattended upgrades, operator users with sudo, OpenSCAP compliance scan."
intent_optimized = "Ansible: CIS L1 hardening, SSH lockdown, firewalld, node_exporter, Filebeat, auto-patches, OpenSCAP report."
over_compressed = "Harden the Linux servers."
assumption_led = [
    "Assuming Ansible available, CIS Level 1 benchmarks, Prometheus node_exporter for metrics, and Filebeat to ELK. Proceeding with fleet hardening.",
    "Correction: centralized logging target is Splunk, not ELK.",
    "Re-executing with Splunk Universal Forwarder instead of Filebeat for log shipping.",
]

[[examples]]
task = "Build me a patient risk calculator."
operations = [
    {name = "Input form: age, prior admissions, department, diagnosis code",                                    type = "python"},
    {name = "Weighted risk score: (prior_admissions\u00d70.4) + (age_factor\u00d70.3) + (diagnosis_factor\u00d70.3)", type = "python"},
    {name = "Color-coded badge: HIGH / MEDIUM / LOW with thresholds",                                          type = "python"},
    {name = "Per-factor formula breakdown below the badge",                                                    type = "python"},
]

[examples.prompts]
verbose = """I need to build a patient risk calculator as a web application. The app needs an \
input form where users can enter patient data including age, number of prior admissions, \
department selection, and primary diagnosis code. Based on these inputs, compute a \
weighted risk score using the formula: (prior_admissions x 0.4) + (age_factor x 0.3) + \
(diagnosis_factor x 0.3). Display the result as a color-coded risk badge — HIGH for \
scores 0.7 and above, MEDIUM for 0.4 to 0.69, and LOW for below 0.4. Below the badge, \
show a formula breakdown with each factor clearly labeled with its weight contribution. \
Use Streamlit for the framework and Python for the implementation."""
clarification_heavy = [
    "Build me a patient risk calculator.",
    "Something for tracking how sick patients are.",
    "I guess like a score, based on their age and history.",
    "Probably a web app, maybe Streamlit?",
    "It should show if risk is high, medium, or low.",
    "The formula? I'm not sure — you can decide.",
    "Just make it look nice and give me the code.",
]
context_aware = "Build a Streamlit patient risk calculator with input form (age, admissions, department, diagnosis), weighted score formula, color-coded HIGH/MEDIUM/LOW badge, and per-factor breakdown."
intent_optimized = """
## Patient Risk Score Calculator
Streamlit app computing a readmission risk score from patient inputs.

### Requirements

1. Input form: age, prior admissions count, department (select), primary diagnosis code
2. Compute risk score: (prior_admissions \u00d7 0.4) + (age_factor \u00d7 0.3) + (diagnosis_factor \u00d7 0.3)
3. Color-coded badge: HIGH (score \u2265 0.7), MEDIUM (0.4\u20130.69), LOW (< 0.4)
4. Formula breakdown below the badge \u2014 each factor labeled with its weight

### Constraints

- Language: Python
- Framework: Streamlit
- Output: generated/code/"""
over_compressed = "Patient risk calculator."
assumption_led = [
    "Assuming Streamlit framework, Python language, and standard weighted readmission risk formula. Proceeding with patient risk calculator build.",
    "Correction: formula weights should be equal (0.33 each), not the provided distribution.",
    "Re-executing with equal-weighted formula where each factor contributes equally to the risk score.",
]
