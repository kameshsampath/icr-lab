I need to deploy a machine learning pipeline on Snowflake using Snowpark Container Services. The pipeline should include a data ingestion stage that reads from an S3 external stage, a feature engineering step using Snowpark DataFrames, model training with scikit-learn running inside a container job on a GPU compute pool, model registration in the Snowflake Model Registry, and a scoring function exposed as a UDF. The compute pool should use GPU_NV_S instance family with min 1 and max 3 nodes. I also need a Streamlit app for monitoring model drift and retraining triggers. The whole thing should be versioned using a Native App so we can share it across accounts. Make sure to set up proper RBAC with a dedicated ML_ENGINEER role that has access to the compute pool, stages, and model registry but not to raw PII data in the source tables.
